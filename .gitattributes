# Deterministic generated contracts and SQL checksums across isolated worktrees.
shared/contracts/generated/** text eol=lf
shared/contracts/fixtures/** text eol=lf
tracking/contract_seed.json text eol=lf
# The capability register is historically committed with CRLF; keep path-only
# evidence updates reviewable without treating each CR as trailing whitespace.
tracking/capabilities.json whitespace=cr-at-eol
database/customer/migrations/** text eol=lf
# Raw terminal evidence retains CR/progress whitespace exactly as captured.
handoffs/codex/artifacts/*.log -text -whitespace
# Approved original: preserve source bytes, including original formatting.
docs/source/ORVIA_Version_1_Unified_Master_with_Version_2_AI_Roadmap.md -text -whitespace

# Preserve imported raw Claude bring-up evidence byte-for-byte.
handoffs/code/artifacts/*.log -text -whitespace
