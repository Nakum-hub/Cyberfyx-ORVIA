# Cowork → Codex: contract, command and test requirements (r3)

| Field | Value |
|---|---|
| From | Cowork |
| Base | `e839b1a` (A00 merged; contract 0.2.0 `PENDING_W00`) |
| Date | 16 Sep 2026 |

Cowork does not edit `packages/contracts`, scripts, manifests or tests. Each item below needs Codex implementation or clarification, then Work review.

## A. Contract (0.2.0 follow-ups)

| Finding | Request | Expected behaviour | Closure evidence |
|---|---|---|---|
| F-024 | Uncertain writes | State explicitly whether any response is a guaranteed pre-acceptance **no-commit** refusal. Today a 503 or timeout carries no such guarantee, so the UI treats it as outcome-unconfirmed. Keep "identical request with the same Idempotency-Key returns the original receipt" for grant and withdrawal. If a request-status lookup is intended, propose it; none exists in 0.2.0. | Contract example(s); A02 replay tests; T08, T09 |
| F-007 | Overview counts | Define `counts.unverified` (which observation states it includes). Propose per-state observation counts if Work requires them. | Contract + T21 parity test |
| F-008 | Representations still missing | Quarantined restored target (T25); ignored stale event in the workflow timeline (T10); "retry permitted" after reconciliation; permission-loss coverage change (T20). No Cowork enum has been added. | Contract examples reviewed by Work |
| F-009, F-025 | Reason codes | A reviewed catalogue mapping `CommandReceipt.reason_code` and `Decision.reason_codes` to safe display text, per purpose. Order-service decisions must not reference marketing consent. | Mapping file; T15 |
| F-011 | Metadata and MFA | `Overview.build_id/profile/contract_version` must equal the build manifest. Confirm the MFA enrolment path under `/api/auth/staff`. | A01/A07 handoff; T30 |
| F-016, F-017, F-018 | Contract gaps (after Work decides) | Receipt list route; notice publish route; portal purpose list rule; staff-visible admission records for T14 | Contract bump + examples |
| F-023 | Auth-library strings | List the Better Auth error codes and messages the UI will receive at sign-in and MFA, so Claude Code can map them to copy | A01 handoff |
| F-002 | Capability records | `GET /admin/capabilities` must be sourced from the genuine `tracking/capabilities.json` (33 modules; IDs 19–25 `DEFERRED_V2`) when it is supplied, never from a generated guess | Register present; T30 |

## B. Operator commands still missing (F-020)

The runbook lists every A00 command as **SUPPLIED** (not run by Cowork or the human). These are still needed, with exact text, working directory, profile, prerequisites and expected output:

- **A01**
  - Protected organisation/owner bootstrap CLI.
  - Application database roles and RLS setup.
  - Staff and principal sign-in URLs.
  - MFA enrolment.
- **A01/A02:** the `aster-birch-v1` business seeder and its non-demo guard.
- **A05/A06**
  - Simulator fault-mode selection (`HEALTHY`, `UNAVAILABLE`, `APPLY_THEN_TIMEOUT`, `ACK_WITHOUT_EFFECT`).
  - Worker interruption and recovery.
  - Quarantined target-only restore.
  - Egress blocking and traffic capture.
  - Full synthetic application reset (T28).
- **A07**
  - Candidate identification and build manifest.
  - Clean start of the full application (web, worker, agent) in the `rehearsal` profile.
  - Application stop.
  - Evidence export location.
  - TLS on exposed paths.

## C. Test evidence Cowork will index

For each T01–T30 record, supply:

| Field group | Fields |
|---|---|
| Identity | `record_id`, `test_id`, `task_id` |
| Run | `run_role` (NORMAL, EXPECTED_DETECTION or HEALTHY_RERUN), command, `exit_code`, start and finish times, `observed_result` |
| Provenance | `code_under_test_commit` (full SHA), `build_id`, `contract_version`, profile, `fixture_id`, `source_handoff` |
| Artifacts | artifact paths and their SHA-256 |

**Rules:**
- **T24:** keep the broken-control FAIL as FAIL, with `expected_fault_detection` set, **and** the healthy rerun.
- **T18 and T19** are mandatory even though the demo may skip them live.
- **Failures:** keep failed runs against their original commit.

Cowork's validator rejects records with missing provenance, hash mismatches, or a mismatched candidate for any claim.

**Forwarding does not close these findings.**
