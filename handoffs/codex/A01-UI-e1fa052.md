# A01 → Claude Code B01 binding handoff

Base `e1fa052c6c419e90c4783ce220dee2ef247472dc`; implementation branch `prototype/codex/A01-auth`. Exact submitted source and evidence commits are recorded in `A01-publication.json` when published. Contract **0.2.1**, unchanged semantic schemas; accepted W00 metadata and `tracking/contract_seed.json` now come from the same generator. UI ownership stays with Claude Code. No UI file was changed.

Implemented: Better Auth staff/principal mounts; `GET /api/v1/session`; `GET/POST /api/v1/admin/principals`. Health remains implemented. Other generated routes remain explicitly pending their tickets. A01 does not implement a login page, policy/configuration UI, consent choices, workflows, evidence exports or Test Lab.

Use existing `staffAuthClient` and `principalAuthClient` in `packages/auth/src/client.ts`. Mounts are `/api/auth/staff` and `/api/auth/principal`, cookie prefixes `orvia.staff` and `orvia.principal`; cookies use `/`, HttpOnly, SameSite=Strict. Use separate browser contexts for staff and principal journeys: two active domains intentionally produce 403. Cookies/secrets from one lane must never be copied to another profile.

For a newly bootstrapped privileged staff account, email/password sign-in authenticates only the enrollment session. Business `session` returns 403 until MFA is complete. Invoke the library's `twoFactor.enable` with the locally entered password, display its returned enrollment URI/recovery codes only to that local user, then call `twoFactor.verifyTotp`. Never log those responses. Existing enrolled accounts receive `twoFactorRedirect: true`; complete the challenge before calling the business session. Do not infer authority from `twoFactorEnabled` or a UI role selector. Invalid MFA gives native 401; unverified-enrollment backup-code attempts cannot establish authority. MFA disable and device trust are unavailable.

Native library paths exposed in each domain: `sign-in/email`, `sign-out`, `get-session`, `list-sessions`, `revoke-session`, `revoke-sessions`, `revoke-other-sessions`; staff additionally `two-factor/enable`, `two-factor/verify-totp`, `two-factor/verify-backup-code`. Public signup and all other auth paths return 404. All auth POSTs require same-origin JSON, bounded bodies and supported fields. Native auth errors are not the versioned API envelope.

Versioned API examples are generated in `packages/contracts/generated/examples.json`. These are synthetic contract examples, not runtime fixtures. For the real directory:

```text
GET /api/v1/admin/principals?limit=25
POST /api/v1/admin/principals
Content-Type: application/json
Idempotency-Key: <new 16–128 character A–Z/a–z/0–9/_/- key>
```

The POST body has exactly `environment_id`, `legal_entity_id`, `display_name`, `email`; scope values come from the actual server session and the email must end in `@aster.example` or `@birch.example`. The generated transport supplies the request shape and validates the 201 `Principal` response. Creation adds a synthetic directory reference, not a login account. Retry the identical body/key to retrieve the same resource; changed payload reuse returns 409 `IDEMPOTENCY_CONFLICT`. New interactions use new keys. The transport does not automatically retry writes.

401 means reauthenticate; 403 means insufficient authority or incomplete privileged MFA; 404 means the selected scope is inaccessible; 400 includes safe field errors for invalid input or duplicate scoped email; 503 means a required dependency/audit was unavailable. Display actual `error.message` and `request_id` safely, with no raw object/stack dumping. Session and directory responses are no-store. They need no background workflow polling. Other polling guidance remains in generated `interfaces.json` and becomes live only with the producing tickets.

The protected synthetic seed writes `.local/profiles/<profile>/auth/bootstrap.json`. Its opaque IDs and generated credentials are local to that lane. Keys `owner`, `admin`, `auditor`, `member`, `alice`, `bob`, `birch`, `birch_principal`, `sibling` identify synthetic role/scope fixtures. `reviewer` is reserved for the incomplete-enrollment security control during A01; it is not a completed policy-approval flow. No default password exists. The auth suite persists fixture TOTP enrollment locally for repeatability; this fixture file must not ship or be uploaded.

Verified Codex commands and limits are in `docs/engineering/A01-AUTH-AND-SCOPE.md`. Codex auth tests start and stop their own Next server; check the publication state before assuming one is running. Claude Code must initialize its separate `ui-b00` profile and use port 4320. That UI profile and actual browser binding tests were NOT_RUN here. Keep the current bootstrap UI until your lane replaces it. Do not call unfinished routes with success-shaped fallback data.

Work review and human integration of A01 remain required. Request any semantic change with producer, consumer, migration and tests; never maintain a private DTO or alternate endpoint.
