# Handoff — A04 — Codex

Base `3ddf9f473f283e68affd24e1f00bf67d04fa808d`, branch `prototype/codex/A04-A07-core`. Contract 0.3.0 unchanged; approved master SHA-256 `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`. Source/commit paths and hashed artifacts are enumerated in `A04-publication.json` after the implementation commit.

Delivered current OPA-backed preview/send admission, separate enrolled sender identity, durable synthetic queue/attempt/decision/send rows, approved order-service fixture, protected seed command, generated implementation metadata and integration coverage. See `docs/engineering/A04-SEND-ADMISSION.md` for caller/scope, transaction/idempotency, epoch/generation, capability, failures and observation boundary. UI ownership is unchanged.

Actual recorded commands (pinned `node scripts/record.mjs`, `ORVIA_TASK_ID=A04`, codex-a00): `install --frozen-lockfile --offline`, `contracts:generate`, `build`, `lint`, `typecheck`, `contracts:check`, and `test` all exited 0; unit suite 11/11. `test:enforcement` initially exited 1 because the test expected 403 for an AGENT credential on a SENDER mount; the established authentication error is 401. That failure is retained. Corrected test exited 0 with 46 real assertions, including durable queue ordering, actual send-row counts, stable replay, wrong authority/scope, independent service condition, and actual OPA missing/malformed/outage recovery. Tests identify dirty source hashes; no exact-commit rerun is implied.

Earlier A04 dependency setup failure, sandbox-limited typecheck, successful migration/setup and source-restoration evidence remain retained. Migration 0007 is applied and immutable. No profile/database reset was performed. All development credentials remain locally generated and protected.

Work review/acceptance remains pending. No browser, real messaging, TLS, egress or production-readiness claim. A05 supplies simulator uncertainty, reconciliation, manual work and evidence; A06 supplies broken-control/recovery/runtime tests; A07 supplies the frozen package. Claude Code consumes the existing generated `evaluate` and `send` shapes; the private send route is for machine callers, never browser authority.
