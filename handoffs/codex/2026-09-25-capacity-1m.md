# A00 / base V1 capacity work — 2026-09-25

**Base commit:** `487a77f573fa1afd8a8b250c5303c58f6942cc29` (working tree already contained other in-progress base V1 changes). **Owner:** Codex. **Boundary:** base V1 paths only; DPDP extension pack remains Claude Code's lane. **Target clarified by user:** at least 1,000,000 total records per customer organisation, across data types.

## Changed paths in this capacity slice

- `database/customer/migrations/0040_capacity_indexes.sql`: scoped chronological indexes for consent, audit, rights, coverage and incident events.
- `scripts/capacity-probe.ts` and `package.json`: isolated, synthetic, million-row PostgreSQL keyset measurement command.
- `docs/engineering/CAPACITY_1M.md`: target, required design and honest acceptance gaps.
- `backend/domain/src/reporting/reports.ts`, `tests/unit/reports.test.ts`: report print ceiling now raises a named validation error instead of an unrelated epoch conflict.
- `frontend/src/components/screens/governance/reports.tsx`: print limit and lack of large export are visible before an operator requests a report.
- `tests/integration/consent/expiry.test.ts`: require a measured blocker and pre-expiry transaction rather than an unstable `pg_stat_activity.wait_event_type` label.
- `scripts/verify-suites.ts`: allow a named `--from` start so an interrupted battery can run its remaining suites with a fresh run ID.

## Verification to date

- `node_modules/.bin/tsc --noEmit`: exit 0.
- `node_modules/.bin/eslint scripts/capacity-probe.ts tests/integration/consent/expiry.test.ts --max-warnings 0`: exit 0.
- `node --import tsx --test tests/unit/reports.test.ts`: exit 0, 9 tests.
- `python docs/reviews/cowork/tools/validate_docs.py`: first exit 1, 97/98 because generated pack was stale; `build_pack.py` exit 0; second validator exit 0, 98/98.
- `git diff --check`: exit 0 before the final edits; rerun at handoff.
- `node --import tsx scripts/migrate.ts` under `ORVIA_PROFILE=codex-a00`: exit 0; applied `0040_capacity_indexes.sql` (artifact `A00-migration-1790309518590-69656842-0b2f-4848-a6cd-07a351457169.json`).
- `node --import tsx scripts/capacity-probe.ts` under the isolated profile: exit 0, artifact `A00-capacity-probe-1790309594947-365288d5-ab3f-4419-83fb-440d0bf4683b.json`. One million scoped rows plus 1,000 in a second scope; 101-row first and deep pages; index scans; 34,298 ms load, 5,410 ms index build, 11.76 ms first page and 4.54 ms deep page on this machine. The transaction rolled back.
- `node --import tsx scripts/web.ts build`: exit 0, 50 routes generated, no demo UI route.
- `npm run test:support` standalone: first exit 1 with zero assertions during scenario; second exit 0 with 57 assertions (artifact `A00-support-integration-1790310113570-1223488a-3e85-4f55-a808-fdddacbb8de3.json`). The earlier failure is not erased.
- Resumed suite battery from updates: exit 1, 10 suites / 354 assertions / one failure (`test:workflows`), artifact `A00-suite-battery-1790311915994-6ae1af90-843f-41f8-bcab-f095be9fa55e.json`. Updates, audit, audit retention, onboarding, languages, preflight, imports, reports and regression passed.
- `npm run test:workflows` standalone: exit 0, 32 passing assertions, artifact `A00-workflow-integration-1790312132911-2eae3835-0757-4c89-b62d-e81803bbbc6c.json`. The earlier battery failure after nine assertions remains recorded.
- `npm run test:expiry` against rebuilt server: exit 0, all recorded assertions passed, artifact `A00-expiry-integration-1790312278798-a55c0f17-b732-49a5-ad8e-ba645ef8e18f.json`.
- `npm test`: exit 0, 201/201 unit tests. `npm run lint`: exit 0. `npm run contracts:check`: exit 0, 8 artifacts and canonical seed, contract 0.18.0. `npm run tracking:check`: exit 0, no acceptance results promoted. Final document validation 98/98, `tsc --noEmit` exit 0, `git diff --check` exit 0.
- Final `node --import tsx scripts/web.ts build` after the report UI copy: exit 0, 50 routes generated.

## Limits and dependency

The probe measures one indexed SQL page path in a temporary table. It does not prove that the API, report, export, ingestion or recovery paths meet the target. The current 2,000-row report print view and 5,000-row audit JSON export remain explicit scale gaps. The end-to-end 1M acceptance status is **NOT_RUN**. The first 27-suite battery was interrupted after repeated zero-assertion failures and produced no final battery artifact; individual suite artifacts retain their results. A resumed battery covered the last ten suites; workflow failed in that battery and then passed standalone. This is not one clean full-battery pass or human acceptance.
