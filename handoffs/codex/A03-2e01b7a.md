# Handoff — A03 — Codex

**Base:** A02 evidence commit `2e01b7a`. **Implementation:** `0341009` (full hashes and exact changed files in [A03-publication.json](A03-publication.json)). **Contract:** 0.3.0, pending consolidated Work review. **Profile:** codex-a00 only.
**Master SHA-256:** `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`.

## Delivered

Real Temporal outbox dispatcher/workflow/activities; immutable scoped plans and signed commands; separate machine authentication; restricted local agent with current-epoch/generation checks; separate synthetic CRM database and durable command/nonce ledger; independent read-only target observation; live workflow API and conservative current receipt status. [Engineering protocol](../../docs/engineering/workflow-and-agent.md) documents roles, expiry, transaction ordering, retry boundary and limitations.

The target update and target receipt commit together. Retrying a known identical command returns its recorded outcome without touching the target again. The tested Temporal-start/database-commit gap safely repeats the stable workflow identity. A stopped worker does not lose accepted work; the test starts the agent, confirms the actual target change, restarts the worker, then confirms a separate read completes the obligation. No provider acknowledgement is promoted to an independent observation.

## Commands actually executed

Via pinned toolchain, `ORVIA_TASK_ID=A03`; every wrapper and child report is indexed with its actual exit code/hash in the publication JSON.

| Command | Exit | Result |
|---|---|---|
| install --offline | 0 | Auth explicitly declares already-pinned Zod 4.6.5; lock updated without version changes |
| db:migrate | 0 | Applied immutable 0006 machine/workflow migration |
| machine:init confirm:codex-a00 | 0 | Generated/protected machine credentials, enrollment and isolated targets; preserved existing restrictions |
| typecheck | 2 then 0 | Fixed dependency/type inference issues; latest root check passed |
| lint / build / contracts:generate | 0 | Real source checks/build and refreshed implementation metadata |
| test | 0 | 11 unit controls including F07 |
| test:workflows | 1, 1, 0, 0 | Corrected enrollment parsing and signal-aware test cleanup; 31 checks then 32 with stale portal status |
| test:consent | 0 | All 49 A02 regression checks remain passing |
| hygiene:check | 0 | 486 candidate files, nine browser asset files, 31 local generated secrets compared; no findings |

The interrupted signal-cleanup run has its real wrapper log and nonzero exit; it did not finish its assertion report. Only the three verified test-owned Node processes were stopped. No volume or database reset occurred. Earlier errors remain in the evidence index. Tests ran before the implementation commit, with exact source hashes in their reports; final frozen-candidate checks remain A07.

## Acceptance and UI handoff

[Workflow integration report](artifacts/A03-workflow-integration-1789574865814-9eda83b4-fffb-4d8a-a1c3-6028345b616a.json): 32 passing assertions for the implemented T10–T13 backend portions. These include real dispatch interruption, worker process restart, SQL target mutation, separate read, duplicate command, changed-payload reuse, wrong trust, expiry, nonce and stale-generation/epoch denials. Additional adversarial generation/approval/schema and service-restart coverage remains A06/A07; no full acceptance is manufactured from this subset.

Claude Code can bind `workflows`, `workflow`, `own_receipt` using the generated client. `ACCEPTED`, execution acknowledgement, observation and aggregate completion remain distinct. A new consent epoch invalidates an old current-completion claim. UI files and browser tests remain Claude Code owned. Worker and agent commands require protected machine setup after mapping creation; one-hour credentials require local renewal. `poll_commands` and `command_receipt` are private machine interfaces, never browser calls.

## Remaining limitations and next action

A04 send admission, A05 real REST faults/reconciliation/evidence/manual work, A06 full backend adversarial/recovery/egress checks and A07 packaging/TLS/frozen-candidate qualification remain. Application transport is development loopback HTTP. Same-user Windows process co-hosting is not a filesystem-isolation qualification. Production/vendor connectors are absent. No universal exactly-once, estate-wide enforcement, browser or release claim.

Continue A04 on the same branch under the human's consolidated-review instruction. Work/human review and human merge remain required; no self-merge.
