# A00 implementation handoff — base e5cdef3

Task A00 only in this increment. Base `e5cdef310d61a697edcf9e4f7bc03c9a4a0a3e8c`, branch `prototype/codex/A00-bootstrap`, customer-local synthetic profile. The attached standing mission permits A00–A07 serially but preserves W00/human integration gates. A01 and B00 have not started. Work-owned task/result files remain unchanged; this is an unaccepted implementation submission, not self-acceptance.

The bootstrap commit and exact publication SHA are recorded in the companion `A00-publication.json` after commit. `A00-changed-files.json` lists every added/changed path against the base, and `A00-command-index.json` indexes raw command reports including failures. Each report carries actual timestamps/exits/source-file hashes. These companion files are generated from Git and executed evidence, not assumed results.

## Delivered and tested scope

Shared TypeScript/Next scaffold, compatible pinned dependencies and lockfile, Drizzle bootstrap migration, locally generated credentials, isolated PostgreSQL/OPA/persistent Temporal services, real Temporal worker probe, safe reset, executable contract 0.2.0 and eight generated artifacts, browser transport and library auth client mounting. Pure domain tests define the completion boundary; they do not claim business workflow persistence. No default login, mock workflow, fake counts, canned runtime verification, real delivery or model/telemetry integration was added.

Real probes exercised PostgreSQL write/read, OPA positive/negative decisions, Temporal worker execution and database/history persistence across service restart. Reset invalid-input guards preserve the probe; the approved exact bootstrap reset exported intent/outcome and deleted only probe rows. Next build and built HTTP liveness/page/404 checks executed. Details and limitations are in `docs/engineering/REPOSITORY_INVENTORY.md` and the command index. T01/T28 are exercised only at A00 bootstrap depth; all full application T01–T34 acceptance remains NOT_RUN in canonical tracking.

The initial-only validator was replaced with progress-aware scripts and unit fixtures. Existing Work JSON passes; valid progress/BLOCKED states pass; cycles, unknown dependencies, missing evidence, forged application PASS from a document/bootstrap subset and Markdown drift fail. Proposed generated views are in `handoffs/codex/generated/`; the scripts do not write Work-owned views. This check is document consistency, not application acceptance.

## Contract and ownership transfer

`docs/engineering/A00-CONTRACT-PROPOSAL.md` is change request `A00-W00-F02-F03` from design 0.1.0 to executable proposal 0.2.0. It defines signed bindings/canonical digests, authorized NOT_REQUIRED policy outcome, separate reconciliation state, immutable receipt replay/current status, manual versus observed completion, exact auth/private/send/test interfaces and isolated profile names. `packages/contracts/generated/manifest.json` records the contract artifact hashes. The seed under generated contracts is a proposed reconstruction, clearly not the absent original. Work must confirm the seed and bootstrap ownership before the missing `tracking/contract_seed.json` is populated.

Codex hereby transfers UI ownership of **`apps/web/src/app/layout.tsx` and `apps/web/src/app/page.tsx`** to Claude Code at this bootstrap handoff, effective when Work/human accepts the shared base for B00. They contain only minimal labelled foundation content. Codex retains `apps/web/src/app/healthz/route.ts`, all API/server/auth paths, Next/build configuration and manifests. No other UI path was authored. Claude Code imports `@orvia/contracts/client`, `@orvia/contracts/generated`, `@orvia/auth/client`; it must not duplicate DTOs or guess endpoints. All business endpoints remain explicitly unimplemented pending their tickets.

After acceptance, the human/authorized UI lane can create an isolated worktree from the accepted bootstrap SHA, for example `git worktree add -b prototype/ui/B00 ../orvia-ui <accepted-sha>`. This is a handoff command, NOT_RUN here; the sibling path needs its own writable-root authorization. Initialize `ui-b00` there and use its distinct ports/database/volumes/Temporal namespace/credentials. Never share the Codex profile. Exact commands/ports are in `A00-LOCAL-DEVELOPMENT.md`.

## Failures, limitations and next dependency

Retained failures include dependency-age rejection, package build-script approval configuration, initially invalid generated examples, a domain type import, Temporal flag/storage setup, internal-network host reachability, a build wrapper that hit the global pnpm shim, later tracker type narrowing and Windows build-cache staging. Recovery and repeat offline installs passed with build caching disabled. Initial staged whitespace checking also rejected raw terminal CR/progress whitespace (exit 2); raw evidence logs now have narrowly scoped Git attributes preserving their bytes, while source whitespace remains checked. Successful reruns do not erase these records. Exploratory commands not wrapped by `record.mjs` are described in the command index; no fabricated exit-code transcript is supplied.

The public advisory audit was **NOT_RUN**. Automatic approval review rejected sending private lockfile dependency metadata to the public advisory service. No workaround was attempted. Local exact-credential/pattern scanning is narrower and is not an advisory or comprehensive security certification. Real auth/MFA/RLS, transactional consent/outbox, restricted agent, CRM mutation, send enforcement, uncertainty persistence, recovery/security suite and candidate packaging remain A01–A07, not completed by this scaffold. TLS, full egress qualification and all production gates remain outstanding. Only codex-a00 was run.

Required review inputs still absent from their named paths: human-controlled approved master under `docs/source/`, original `tracking/contract_seed.json`, `tracking/capabilities.json`, `handoffs/TEMPLATE.md`, `prompts/02_CODEX_BACKEND.md`. The separately supplied master is hash-verified and the attached prompt supplies role guidance, so independent A00 work proceeded. Human supplies the source placement/deadline; Work restores its shared template/role input if required; Cowork owns capability inventory. Do not import an older kit to fill these gaps.

**Next dependency: W00 reviews this exact code and evidence, records accepted semantics/status, then the human integrates the branch.** Retest relevant checks on that integrated commit before A01/B00 proceeds. This branch is pushed under the user's explicit authorization; Codex does not merge main, publish a site or release the prototype.
