# Handoff - V1-EXPANSION-03 - GRC API and workspace

Base commit: `487a77f573fa1afd8a8b250c5303c58f6942cc29`. New commit: not committed; pre-existing changes preserved. Customer contract: 0.26.0. Approved master SHA-256 reverified: `c51102a7cda5fe15c1346e8c34167c406e186c691e9ba86576a3d8fd03bb550b`.

## Delivered

Fourteen canonical staff routes integrate the existing framework/control/evidence/risk domain. Evidence and treatment history are independently paginated and scoped to their parent record; replacement never inherits an old review. Server roles, OPA policy, origin checks, MFA, durable idempotency, RLS and request auditing apply. Framework request size is explicitly bounded at 262144 bytes, controls at 65536 bytes; other routes retain their existing limit. Generated OpenAPI documents the limits.

The workspace uses canonical generated types and real API calls. It includes versioned frameworks, searchable requirement mappings, control evidence submission and independent review, risk proposals and reviews, and immutable histories. Auditors see read-only views. Manual acceptance never claims automated effect verification. Test-only dependency binding makes production request handlers testable against an isolated runtime without request-controlled injection or audit bypass.

Changed paths: `shared/contracts/src/{index,examples,generate,grc}.ts`; generated contract artifacts and `tracking/contract_seed.json`; `backend/api/src/{business,http,governance-routes,grc-routes}.ts`; `backend/authorization/src/index.ts`; `backend/policy/admin/authorization.rego`; `backend/domain/src/grc/{grc,risks}.ts`; `frontend/src/components/screens/governance/grc.tsx`; `frontend/src/app/workspace/grc/page.tsx`; `frontend/src/components/shared/shell.tsx`; `tests/integration/grc/{http.test,browser-harness}.ts`; expansion tracking, active-lane note and this handoff. Shared files contain earlier work; this list does not attribute their entire diff to this task.

## Commands actually executed

| Command | Exit | Result / evidence |
|---|---|---|
| `node node_modules/tsx/dist/cli.mjs shared/contracts/src/generate.ts --check` | 0 | Eight generated artifacts, canonical seed, 188 routes, seven error examples validated |
| `ORVIA_GRC_OPA_PORT=4494 BETTER_AUTH_TELEMETRY=0 node node_modules/tsx/dist/cli.mjs tests/integration/grc/http.test.ts --browser` (PowerShell environment assignments) | 0 | 40 checks; `artifacts/V1-EXPANSION-03-http-41db8841-9921-463c-bc76-840d0229c706.json` |
| Local ESLint over changed API/domain/GRC contracts/UI/tests with `--max-warnings 0` | 0 | No findings; rerun after Unicode fixture correction |
| Root typecheck, full unit suite and production web build in preceding continuation | 0 | `artifacts/V1-EXPANSION-03-typecheck-verified.log`, `V1-EXPANSION-03-unit-final.log` (242 pass), `V1-EXPANSION-03-build-final.log` |
| Playwright CLI `goto`, `snapshot`, `click`, `run-code` against isolated browser fixture | 0 | Control mapping/filter persistence, evidence submission, independent review, risk creation, treatment proposal/review, auditor read-only journeys executed against real API/database |
| Playwright mobile viewport 390x844 and desktop screenshot | 0 | Page width 390; `output/playwright/grc-auditor-mobile.png`, `grc-auditor-desktop.png`; mobile visually inspected |
| `pnpm exec tsx ...` and `pnpm contracts:check` | 1 | Package-manager registry signature lookup failed under restricted network. No bypass flags used; installed local tools ran directly |

The earlier payload labelled Unicode actually contained question marks after shell encoding. It was corrected to an explicit Unicode escape and the full 40-check HTTP suite rerun. Only the final HTTP artifact above supports the Unicode assertion.

## Acceptance and limits

HTTP checks cover MFA, anonymous/principal/auditor denial, cross-tenant records/mappings/history, strict bodies, origin, idempotency replay/conflict, independent reviewers, replacement evidence, revoked-authority replay and fail-closed audit failure. Browser evidence uses the production component, generated client, authentication sessions, API handlers, policy and database. The harness is test-only and does not constitute full Next shell/login/routing acceptance.

Two initial browser list reads returned 503 dependency errors. Explicit retry recovered both; subsequent reviewer/auditor loads were healthy. Cause is not established, so a clean first-load/performance qualification is not claimed. Historical UI framework creation was tested on an earlier isolated fixture. Browser fixtures contain synthetic records only.

Full EX10 acceptance remains NOT_RUN: audit engagements/requests, policy lifecycle, regulatory content and verified mitigation outcomes remain open. No competitor parity, certification or production readiness is claimed. Other expanded baseline families remain in progress/open, including commercial activation and full delivery qualification.

## Runtime and next work

The isolated HTTP server exited 0; the dedicated Playwright session and `orvia-grc-expansion03-opa` container were stopped. Synthetic databases are retained; no shared services or databases were reset. DPDP pack untouched. Continue audit engagement/evidence-request implementation under the active one-writer reservation, then integrate and test before updating acceptance.
