# Handoff — A01 — Codex — implementation submission

**Base commit:** `e1fa052c6c419e90c4783ce220dee2ef247472dc` (human integration of W00 acceptance).
**New commit:** see `A01-publication.json` for the committed implementation and exact post-commit evidence; this handoff is included in that implementation.
**Source master / hash verified:** revision 1.3; SHA-256 `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`.
**Contract version:** `0.2.1`, accepted by W00; no schema semantics changed.
**Scope and profile:** A01 only; `CUSTOMER_LOCAL_SYNTHETIC`, `codex-a00`, `aster-birch-v1` identity fixtures. A02–A07 are not implemented by this submission.

## Delivered

- Separate Better Auth staff/principal servers with the supported Drizzle adapter, unique generated credentials, real privileged TOTP MFA, logout/revocation, persisted rate limits, separate cookie/signing authority, bounded same-origin auth mounts and no public signup.
- Session-specific MFA proof, including rejection of password-only sessions and recovery-code use from an unverified enrollment. Real negative regression retained.
- Protected CLI owner bootstrap and optional synthetic identity seed. Separate migrator/auth/application roles; non-owner/non-superuser/non-BYPASSRLS application access; composite scope constraints; FORCE RLS and transaction-local pooled scope.
- Server-derived session/capabilities; distinct administrative OPA namespace; scoped, paginated principal reference directory and idempotent transactional creation. Stable generated-client interface, safe errors, persisted scoped audit and payload-free denied-request audit.
- A real security suite using production Next HTTP, Better Auth, PostgreSQL and OPA, application restart, a bounded OPA outage/recovery and no canned responses.
- Generated W00 acceptance metadata and accepted seed from the canonical generator; no handwritten second contract. Initial applied migration remains immutable; follow-up migrations preserve its history.

Implementation paths: `.gitattributes`, `package.json`; `packages/auth/src/{bootstrap-password,config,server}.ts`; `packages/authz/src/index.ts`; `packages/db/src/{auth-schema,runtime}.ts`; migrations `0001_auth_scope.sql`, `0002_idempotency_key_bound.sql`, `0003_request_audit.sql`; `packages/contracts/src/{index,generate}.ts` and generated metadata/OpenAPI/manifest; `tracking/contract_seed.json`; `packages/testing/src/evidence.ts`; `policy/admin/authorization.rego`; auth, session and principal API route handlers; `apps/web/src/server/{http,runtime}.ts`; auth/bootstrap/private-file/migration/record/hygiene/web scripts; `tests/security/auth.test.ts`; `tests/unit/tracking.test.ts`; `docs/engineering/A01-AUTH-AND-SCOPE.md`; this lane's A01 handoffs/artifacts. `A01-publication.json` enumerates exact committed paths. No UI, canonical task/acceptance ledger or Work/Cowork document was edited.

## Commands actually executed

All recorded commands ran from `C:\Cyberfyx-projects\Cyberfyx_ORVIA` using local Node 24.21.0 and pnpm 12.4.2, with `ORVIA_TASK_ID=A01`. Full commands, timestamps, source hashes, dirty identity and raw output are in uniquely named `handoffs/codex/artifacts/A01-*.json/.log`; publication indexes them without discarding failures.

| Command / operation | Exit | Result |
|---|---:|---|
| Git fetch / branch from approved `origin/main` | 0 | Verified human PR #6 merge, created `prototype/codex/A01-auth` |
| Docker status in default sandbox / approved retry | 1 / 0 | Sandbox denied daemon access; normal approval succeeded |
| `services up`; load new OPA policy | 0 | Only existing `codex-a00` named services started/restarted; no reset |
| `db:migrate` (initial + follow-up migrations) | 0 | Real transactional migration, original checksum retained |
| `auth:init confirm:codex-a00` | 0 | Generated protected credentials, scoped runtime grants |
| `auth:bootstrap confirm:codex-a00`; `seed:auth confirm:codex-a00` and seed rerun | 0 | Real persisted identities/authority; no default or printed password |
| `contracts:generate`; `contracts:check` | 0 | Eight generated artifacts plus accepted seed; 37 route and seven error examples validated |
| First `test:auth` | 1 | Host canonicalization defect detected, preserved as a failed execution |
| Subsequent `test:auth` | 0 | 62, then 81, then 87 assertions; final committed retest is linked in publication |
| `test` before / after tracking-fixture correction | 1 / 0 | 10/11 then 11/11; fixture now explicitly creates an unmet dependency after A00 acceptance |
| Build before / after request-ID type fix | 1 / 0 | Type mismatch detected and corrected; successful production build retained |
| `typecheck`; `lint`; `tracking:check`; `web:smoke`; `preflight`; `hygiene:check` | 0 | Actual engineering checks; final exact-commit reports supersede intermediate source snapshots |

The latest pre-commit HTTP evidence is `artifacts/A01-auth-security-1789568372933-d00cdb92-9e49-4671-8eae-d830ecc922a5.json` (87 assertions; build `FJjI_kSkuOP7W57gsFrgY`). The initial failing HTTP report, unit failure and type-checking build failure remain in the evidence index. These are repaired failures, not expected fault detections. The intentional OPA outage is a passing fail-closed control, not the A06 T24 broken-sender scenario.

## Acceptance

Evidence supports the A01 portions of T02/T03/T04/T05/T27: real login/MFA, generated bootstrap, server scope, tenant/sibling/principal denial, role denial at HTTP and RLS, cookie/domain separation, revoked-session denial, persisted records/session through restart and safe dependency failure. Full test IDs remain under Work's canonical ownership; no status was automatically promoted. Staff-as-principal is tested against the shared authority guard; own-consent HTTP routes are A02, not claimed here. Distinct exact-digest policy approval is also A02. Jobs, commands, counts/exports and application recovery require their later producing tickets and real tests.

T29/browser journeys, UI bindings, TLS, runtime egress, full recovery/reset, dependency advisory audit and rehearsals were NOT_RUN for A01. The previous public advisory upload approval block remains unresolved; no audit result is fabricated and dependencies are unchanged. No new permission was requested to bypass that block.

## Contract / dependency / ownership changes

Semantic contract stays 0.2.1. The Work review explicitly allowed Codex to refresh accepted labels and populate the accepted seed; generator and drift check now do both. Only health, session and principal list/create have implemented route metadata. No dependency pin/lockfile change. All new database migrations are Codex-owned. The audit table documents its installation-local, identifier-free scope strategy; no global audit read is granted to runtime.

UI handoff: `A01-UI-e1fa052.md`. Verified commands, authority/transaction boundaries and limitations: `docs/engineering/A01-AUTH-AND-SCOPE.md`. UI ownership remains Claude Code. Canonical ledger update proposal: set A01 to IN_REVIEW referencing this source/evidence, not COMPLETED until review and human integration.

## Remaining limitations and blockers

- Work must review the A01 authority/RLS/MFA implementation and exact evidence; the human owns integration. No A01 acceptance or full W01 decision is asserted by Codex.
- Development transport is loopback HTTP and local private database transport; TLS/egress readiness is not qualified. No public deployment or customer records were used.
- Protected synthetic credential journal is not a production recovery/secret-management product. Specialised role management, last-owner transfer/recovery, machine enrollment, consent/policy/workflows and other surfaces remain explicitly unimplemented.
- `reset:bootstrap` refuses the new business schema. Do not reset; A06 must implement durable application drain/guard/export.
- Auth stores are pre-authentication installation identity stores with disjoint grants. Business RLS assumes trusted server authority derivation and does not protect against an already compromised app process or database administrator.
- The HTTP suite stops its owned Next process; the exact final service state is recorded in publication. Browser/UI work was not taken over.

## Next integration action

Review the published `prototype/codex/A01-auth` commit and evidence. Work supplies the bounded A01 acceptance/ledger update; the human integrates it. Re-run relevant checks on that integration, then start A02 from the recorded accepted base. W01 still reviews A01 and A02 together before its own completion. A03–A07 retain their existing graph; A07 additionally needs W02 and UI/human rehearsal evidence. Codex has not self-merged, released or accepted later tickets.
