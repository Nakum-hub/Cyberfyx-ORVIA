# Handoff — A05 — Codex — d94f525

**Base:** `e1d80916a3e53b210d2fd90aebba7eaf098fdfee` (A04 publication; initial A05 start note used `9fee18d`). **Implementation:** `d94f525e19d6eaab0999b838a3ac9debef9860a9`. **Master SHA-256:** `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`, reverified. **Scope:** customer-local synthetic `codex-a00`, fixture `aster-birch-v1`. **Contract:** transport 0.4.0; signed command schema remains 0.3.0. Pending Work review.

## Delivered

Actual REST restriction/fault behavior, independently authenticated observer reads, retained uncertain attempts, durable Temporal read reconciliation, immediate capability-loss invalidation, exact MEMBER assignments/manual attestation, scoped persisted overview/failures, and audited local evidence exports. Full boundaries and installer ordering are in `docs/engineering/evidence-and-reconciliation.md`. `A05-publication.json` enumerates all 48 implementation paths, exact commits, commands/exits and hashed artifacts. No UI or Work state/review/tracker file was edited.

## Commands actually executed

All recorded commands use the pinned `node scripts/record.mjs <script>` with `ORVIA_TASK_ID=A05` in this workspace. Raw wrappers contain full command, timestamps, source file hashes, dirty state, fixture/profile and exit code.

| Command | Exit | Result and evidence |
|---|---|---|
| `db:migrate` | 0 | Additive control migrations 0008–0012; every applied version/checksum retained |
| `roles:init confirm:codex-a00` | 0 | Existing-profile protected role bootstrap; no reset or credential replacement |
| `contracts:generate`, `contracts:check` | 0 | Eight generated artifacts, 41 route examples, seven error examples; 0.4.0 |
| `build`, `typecheck`, `lint`, `test` | 0 | Production build; 11 unit controls including W00-F07 |
| `test:evidence` | 0 | **63/63** final assertions; `artifacts/A05-evidence-integration-1789587916158-d22b2447-4862-41b6-900a-777f74fe931c.json`, build `tR-UmUxDbxZfmlVEJzTH3` |
| `test:enforcement` | 0 | **46/46**; `artifacts/A05-send-enforcement-1789588044769-1a53f93c-0d58-4036-856f-bf60aff7365d.json` |
| `test:consent` | 0 | **50/50**, including control-map pagination across the actual retained dataset; `artifacts/A05-consent-integration-1789587443480-4fdc73ea-e587-4f7a-a243-8822e62a3f9e.json` |
| `test:expiry` | 0 | **87/87**, 15 confirmed lock waits; `artifacts/A05-expiry-integration-1789587713035-a93a36be-d2e4-4e5f-baef-dbac1f6ab350.json` |
| `test:workflows` | 0 | **32/32** real CRM/restart/command controls; `artifacts/A05-workflow-integration-1789587147791-3f12c8a7-fa62-4ab5-a925-a7119e3cacc5.json` |
| `test:auth` | 0 | **87/87**; `artifacts/A05-auth-security-1789587212695-4edfcef2-5dcb-44d0-826b-eb40836d44c1.json` |
| `hygiene:check` | 0 | No matching generated credentials/private keys in publishable files or browser bundle; bounded check only |

The earlier workflow/authentication regression passes preceded the final pagination/capability-projection edits. Final A05, send and expiry results identify their tested source hashes; this is not a claim that every suite was rerun against the final commit. The final A05 run exercises the new workflow/capability paths. Post-commit static/hygiene results are separately identified in the index.

Original failures are retained: target-agent permission error after restrictive MEMBER policies (fixed by additive 0010); intermittent HTTP setup TypeErrors; two workflow setup MFA failures; an expiry setup interruption after 17 successful checks; and a send-test setup TypeError. Setup helpers now await subprocesses without blocking the HTTP event loop and emit safe phase/status/source diagnostics. Subsequent runs passed. The exact causes of earlier generic TypeErrors/MFA failures were not conclusively captured; no runtime authentication/rate-limit checks were relaxed. These are ordinary failed/error runs, not expected broken-control detection. T24's actual broken-control experiment remains A06.

## Contract / UI handoff

Change **A05-C01** adds `cursor`/`limit` and required nullable `next_cursor` to `control_map`, avoiding the old rejection above 100 mappings. Claude Code must consume generated contract 0.4.0 and follow the cursor. Command cryptography remains version 0.3.0 so queued command signatures are preserved. No new UI DTO or endpoint is required. A05's already-frozen reconciliation, attestation, failure, overview, evidence/export, system-check and simulator surfaces are implemented; generated metadata remains pending Work review.

Fresh installation now requires `roles:init confirm:<named-profile>` before `db:migrate`, followed by auth/bootstrap/fixture/machine initialization. Existing-profile execution is verified; clean-profile qualification belongs to A07. Target migrations 0002–0003 are applied by the protected machine initializer. The published provider lives in the local Next process; target data/credentials are separated by database roles, not by an independently qualified provider process.

## Limitations and next action

One automatic effect attempt, then explicit read reconciliation/escalation; no blind retry of unknown effects. Manual attribution never substitutes for a required independent read. Evidence `tests` remains empty until A06 creates actual persisted test runs. Connector catalog test status stays NOT_RUN. No browser, runtime egress, TLS, target restore, clean install, two rehearsals, legal compliance or production-readiness acceptance is inferred. A06/A07 and Claude Code/human portions remain outstanding. Private operator fixtures have no arbitrary URL/SQL input; no database reset or real messages were executed.

Human reviews/merges the A05 PR; Work reviews semantics and evidence without this lane self-promoting A05/W02 acceptance. Continue the authorized A06 implementation after publishing this increment. The test-owned web/worker/agent processes exit during cleanup; this handoff does not claim an application server remains running.
