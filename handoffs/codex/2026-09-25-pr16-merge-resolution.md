# PR16 merge resolution

Task: MERGE-16. Feature base: `82aa60da145fc0739b5416c161f52e08c22b4a94`. Target: origin/main at `d9375ca`, containing Claude's DPDP implementation from PR15. User explicitly requested resolving merge conflicts. Resolution is on `prototype/codex/A06-evidence-hardening`; main is not directly changed or merged by this task.

## Resolution

Combined the DPDP operations dispatcher, regulatory/registry/operations schemas and capabilities with GRC, discovery and AI-governance work. Retained server-owned runtime binding, audit, workflow evidence downloads, independent approval roles and both branches' request limits. DPDP's two 1 MiB body limits now live in the canonical route metadata, alongside GRC's bounded framework/control limits. Generated all eight contract artifacts and canonical seed at version 0.28.0: 300 unique routes with no method/path collisions. No migration numbering collision was found; DPDP migrations 0037-0039 precede expansion migrations 0040-0050.

Manually resolved `backend/api/src/business.ts`, `backend/authorization/src/index.ts`, `backend/policy/admin/authorization.rego`, `shared/contracts/src/index.ts`, and `shared/contracts/src/examples.ts`. Regenerated conflicts in `shared/contracts/generated/*` and `tracking/contract_seed.json`. Added canonical limit metadata to `shared/contracts/src/operations-routes.ts`. Added `tests/unit/merged-capabilities.test.ts` and updated five prior unit route-inventory expectations for the DPDP additions. The deployment boundary test explicitly constrains local workflow cancellation by route, method, authority, capability and schemas instead of confusing it with commercial subscription cancellation. Existing authorization assertions remain in place.

## Executed checks

| Check | Exit | Result |
|---|---|---|
| Local TypeScript `tsc --noEmit` | 0 | `artifacts/MERGE-16-typecheck.log` |
| Full local ESLint with `--max-warnings 0` | 0 | `artifacts/MERGE-16-lint-final.log` |
| Full unit suite | 0 | 252 pass, zero fail: `artifacts/MERGE-16-unit-final.log` |
| Contract generation/check | 0 | 300 route examples, seven error examples, eight artifacts and seed |
| Production Next build | 0 | `artifacts/MERGE-16-build.log` |
| Cached OPA image `check --strict /policies` | 0 | Dedicated one-shot read-only container, no shared services changed |

Initial unit run had five failures caused by the old route inventories and cancellation keyword match; retained in `artifacts/MERGE-16-unit.log`. Fixed expectations were rerun rather than suppressed. Source-master SHA-256 remained `c51102a7cda5fe15c1346e8c34167c406e186c691e9ba86576a3d8fd03bb550b`. A temporary encoding conversion in twelve contract comments was corrected; runtime schemas and generated bytes were unaffected by that comment correction.

No database migration, reset, deployment, real-source action or comprehensive browser/integration qualification was executed for this merge. Existing product acceptance limitations still apply. Next dependency: GitHub reports this updated PR mergeable; human PR approval/merge remains separate.
