# Handoff - A07 - Codex - e374030

**Branch start:** `8d852bd2da87dbd41a8cf3e5fa4b400b91cf8360`. **Human integration included:** `f53dd57cf9f6805de891298ed931e632b26e6788` (A06 PR22), incorporated on the feature branch at `bfa81e59b8f525a1d2531ae7b5e7504bb8b42687`. **Frozen candidate:** `e3740308fe217a59d21a68e899a93d07a4829463`; implementation `dfa965c`, UI preservation `04a1666`. Later publication commits add evidence/handoffs only; see [publication](A07-published.md).

**Approved master:** 850,752 bytes, SHA-256 `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`, reverified unchanged. **Generated contract:** 0.4.1; signed commands 0.3.0. **Profile:** customer-local synthetic `rehearsal`, installation `b4a58f9b-7f5e-4e7b-b057-2fa6d68352f4`, fixture `aster-birch-v1`. **Acceptance:** pending Work; no W01/W02/W03 or human gate promoted.

## Delivered

Protected local bootstrap, fresh rehearsal credentials, verified loopback HTTPS, explicit setup/resume/start/stop/demo/export/package commands, persisted service restart and guarded reset evidence. The supervisor holds a profile database lock, checks existing processes/ports, and stops only its own children through a protected run identity. Nonzero/forced shutdown is a failure. The worker drains its dispatch loop before Temporal shutdown. Existing transaction, epoch/generation, signed-command and observation boundaries remain intact.

Preserved all 20 unpublished Claude project/evidence files without restyling or modifying their original worktree. Their hashes match the original, this checkout and the final offline bundle. Ownership transfer and provenance are in [the start note](A07-start-8d852bd.md) and [UI inventory](A07-ui-preservation.json). Staff workspace/sign-in and B/browser acceptance remain unfinished; this is preservation, not B completion.

The [exact publication index](A07-publication.json) lists changed paths, commits, every retained command/exit, source identities and artifact checksums. There are 58 changed non-handoff paths against human main f53dd57, including the 16 preserved UI source files. No existing migration bytes were changed and no new business migration was added by A07. Work-owned acceptance/tracking/review documents were incorporated only through human-merged main.

## Commands actually executed

Unless stated otherwise, commands below ran as `node scripts/record.mjs <script>` with pinned Node 24.21.0/pnpm 12.4.2, `ORVIA_TASK_ID=A07` and `ORVIA_PROFILE=rehearsal`. The index preserves earlier attempts; this table gives successful results. Logs and reports live in `handoffs/codex/artifacts/`.

| Command | Exit | Observed result |
|---|---:|---|
| `install --frozen-lockfile` | 0 | Root and fresh offline-bundle checkout; pinned tool bootstrap also executed in the fresh checkout |
| `profile:init rehearsal`, `tls:init confirm:rehearsal`, `services up` | 0 | New isolated profile, protected generated material and actual private local services |
| `roles:init confirm:rehearsal`, `db:migrate bootstrap-only`, `services:smoke` | 0 | Four bootstrap/service controls, including actual PostgreSQL and Temporal restart with persisted state/history |
| `reset:bootstrap rehearsal rehearsal-bootstrap-only` | 0 | Exported intent first; removed exactly one synthetic bootstrap probe before business migrations |
| `db:migrate`, `preflight`, `auth:init confirm:rehearsal`, `auth:bootstrap confirm:rehearsal`, `seed:auth confirm:rehearsal` | 0 | Real migrations through 0014, protected owner and scoped synthetic fixtures |
| `machine:init confirm:rehearsal`, `seed:orders confirm:rehearsal`, `regression:init confirm:rehearsal` | 0 | Protected machine/test enrollment; initial conditional order seed correctly created zero orders before eligible policy existed |
| `contracts:generate`, `contracts:check` | 0 | Eight generated artifacts/canonical seed; 41 route examples and seven error examples, version 0.4.1 |
| `typecheck`, `lint`, `test` | 0 | Static checks and 11 unit controls |
| `build`, `test:tls`, `test:auth` | 0 | Final e374030 production build; 12 TLS and 87 auth controls |
| `test:consent`, `test:expiry` | 0 | 50 and 87 controls; expiry includes 15 actual lock-wait cases |
| `test:workflows`, `test:enforcement`, `test:evidence` | 0 | 32, 46 and 63 controls respectively; workflow rerun on final e374030 |
| `test:regression` | 0 | 70 integration controls; real healthy/broken/repaired/restoration/interruption results retained |
| `demo:run confirm:rehearsal` | 0 | Actual healthy six-assertion backend demonstration and audited export |
| `evidence:export confirm:rehearsal 8f5e3920-b036-40f8-b400-ef8d0ce08758` | 0 | Standalone authenticated, scoped, audited export of that real synthetic workflow |
| `test:lifecycle` | 0 | Final e374030: 17 controls, two application start/stop cycles; all six child exits 0, none forced; business history retained |
| `runtime:build`, `test:network confirm:rehearsal` | 0 | Final e374030 image; 13 network boundary controls plus real six-assertion backend core with outbound DNS/direct external access blocked |
| `candidate:package confirm:rehearsal` | 0 | Actual source bundle/ZIP, evidence ZIP, image TAR and release manifest |
| `python handoffs/codex/artifacts/A07-package-verifier.py` | 0 | All package checksums; actual offline clone and Git fsck; 1,221 source files, 180 evidence files and all 20 preserved files byte-verified |
| `dependencies:check` | 0 | 498 package entries against the downloaded 7,408 reviewed npm advisory feed; zero matches, not comprehensive vulnerability certification |
| `hygiene:check`, `git diff --check` | 0 | Local generated-credential/private-key scan and whitespace checks; exact scan counts in raw logs |

The final build/TLS/auth/workflow/lifecycle/image/network/package wrappers record committed e374030, no dirty source and no source changes during execution. The full consent/expiry/send/evidence/regression suite ran over HTTPS on bfa81e5 before subsequent lifecycle/package/test-report refinements; it is not represented as rerun wholesale on e374030. Final worker entry-point changes received workflow/lifecycle/network reruns. The earlier fresh offline bundle bfa81e5 also completed pinned bootstrap, frozen installation, contract check and production build; final e374030 bundle received the separate byte/integrity checks above. `setup-rehearsal.ps1` was parsed; its constituent commands executed individually on the fresh profile, not as a second end-to-end wrapper invocation against an existing profile.

## Original observations and corrections

All ordinary failures remain in the index. Initial contract generation failed under protected-tool sandbox access and succeeded through normal approved execution. No permissions were bypassed.

The first lifecycle readiness test incorrectly expected an idle agent database session. Actual evidence showed 75 authenticated successful machine polls; the corrected predicate checks a new successful machine-poll audit and the worker database session. A subsequent readiness timeout retained its failure; host contention was possible but not established as the cause. That run's old supervisor artifact `A07-application-lifecycle-1789615728529-69b510f5-b879-4461-a636-9d2efce8c543.json` incorrectly labelled PASS despite child exit 1 and is **superseded, not acceptance evidence**. The supervisor now fails on any nonzero/signal/forced child exit.

The next run reached real readiness and then exposed `PromiseCompletionTimeoutError`: Temporal was shut down before the outbox-loop promise settled. The entry point now owns signal ordering and lets `runUntil` stop the worker after the loop settles; errors are not suppressed. Final corrected report: `A07-lifecycle-integration-1789617047440-555b379c-efd6-4e3f-9ce3-8051c83c37fa.json`, 17 PASS, with two separate supervisor artifacts showing all children exit 0.

The independent package verifier initially used `hashlib.file_digest`, unavailable in this host's Python 3.10.0. Its original exit 1 is retained. Streaming SHA-256 fixed the verification helper; the successful clone/byte verification did not change any frozen source/package bytes. Python is only used by this independent verification helper, not the application/setup runtime.

Expected broken-control results remain distinct: the deliberately broken business run is FAIL with a real isolated send violation; healthy and repaired runs pass, restoration is actually observed, interruption remains ERROR. No canned result, ledger reset or acceptance promotion replaced these observations. Earlier TLS reports' T29 label and auth reports' inherited HTTP wording are superseded by corrected final TLS/auth metadata; browser T29 remains NOT_RUN.

## Package identity and use

Use [the tested engineering commands](../../docs/engineering/local-packaging-and-operation.md) and [release manifest](../../artifacts/release-manifest.json). Local archives are under `.local/releases/e3740308fe217a59d21a68e899a93d07a4829463/`: `orvia-source.bundle`, `orvia-source.zip`, `orvia-evidence.zip`, `orvia-local-images.tar`, and a manifest copy. The manifest records their SHA-256 hashes and actual service version outputs. These large local archives, credentials, populated stores and tool caches are not Git source; publishable source/docs/synthetic reports are pushed.

Host build `kIB5gxhIwn42T6Dqsburh`; Linux image build `lBTMG6QFakbJa4pG--aN9`; image `sha256:a5f61ea21a6bb667955ac208d0874ca58fb10487e257ac53e2a7553cf69389ed`; image source SHA-256 `66afd7f85927cb81abe61a23cdcab6c97520f88a76aad8f8403a183768d2b111`. Lockfile SHA-256 `b2694da120310a26a6ae377ebecbd4dafcac6711b60377c4ff308fe4fc91a5c8`. Contract manifest SHA-256 `7415cd5aaca0fd243a41fb4347d5a1a2eeb344ca7f0bd737bebb1c213caad62c`.

Actual services: PostgreSQL 17.11; OPA 1.20.2; Temporal CLI 1.9.1/server 1.32.0; Node 24.21.0. All image digests are in the manifest. Development checksums are not production release signatures. The bundled runtime image is the qualified backend test image; documented application startup uses the source package. Frozen dependency provisioning may still require authorized downloads; an air-gapped fresh host is not claimed.

## Contract / dependency / ownership

Shared transport 0.4.1 adds the verified HTTPS staff/principal cookie prefixes; auth mounts, business DTOs, endpoint paths, signed-command 0.3.0 bindings and seed/reset names remain stable. [Producer/consumer coordination](A07-C01-TLS-contract.md) remains pending Work semantic acceptance. Exact TLS-tool dependencies `@peculiar/x509` 2.1.0 and `reflect-metadata` 0.2.2 are pinned in the single lockfile. No stack replacement or private ADR was introduced.

## Remaining limitations and next integration action

Work reviews the A02 expiry correction and A03-A07 evidence in dependency order; W01/W02/W03 remain its decisions. Human owns main integration and release. Preserved UI remains partial: staff workspace/sign-in, unfinished B tasks and browser acceptance are not complete. The transferred B/UI paths remain Codex-owned for their next assigned increments; no absent Claude session is treated as a dependency. Two real human T30 rehearsals remain NOT_RUN. Do not declare the entire prototype/demo READY from this package.

Per-process CA trust is verified; system/browser trust was not installed and browser certificate bypass is not allowed. Certificates are short-lived and machine enrollment expires after one hour; documented protected renewal preserves inactive identities and accepted restrictions. Other development profiles remain explicitly unqualified HTTP. Networking evidence covers the private backend container, not host/browser-wide egress. Full control-plane disaster recovery, production signing/security/legal readiness and public deployment are not claimed. Business reset is deliberately refused and tested unchanged; only the earlier named bootstrap probe was reset. No customer records, real messages, production secrets, paid service or public deployment was used. A08 was not started.

Review/merge the A07 PR, keep this exact engineering candidate and evidence identity, then finish the outstanding B/browser and human rehearsal gates before Work's final recommendation. Any source-changing fix requires a new candidate and relevant reruns.
