# Handoff — A06 — Codex — 3c2ee18

**Base commit:** `62ece0b504edd5e8a405266f06adc763da808cea`. **Implementation:** `3c2ee18f568cebb3c4add734c6c94a3f68e2c692`. **Master SHA-256:** `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`, reverified. **Contract:** transport 0.4.0; signed commands 0.3.0. **Profile:** customer-local synthetic `codex-a00`, `aster-birch-v1`. Pending Work review; no acceptance promotion.

## Delivered

Durable scoped test-run requests, separately persisted assertions, immutable terminal results and real evidence links. Protected operator execution checks current authority/profile/build/contract and exclusive fixture ownership. Healthy and repaired runs pass; the deliberately broken sender produces a real isolated output row and remains FAIL. Interrupted execution becomes ERROR. Target-only restoration enters quarantine at a new generation and requires current-ledger signed reconciliation and independent observation before activation. Added forced RLS for operator-only fault output after the real auth invariant detected it was missing.

The isolated backend container completed an actual authenticated HTTP/PostgreSQL/OPA/Temporal/signed-agent workflow while outbound DNS and direct external-network access were denied. This is backend boundary evidence, not browser/host egress certification. The local advisory matcher downloads the public reviewed npm feed without uploading dependencies. A selective esbuild override removes the observed advisory; deprecated transitive tooling remains disclosed.

Exact 31 implementation paths and all command/artifact checksums are in `A06-publication.json`; behavioral boundaries are in `docs/engineering/regression-and-recovery.md`.

## Commands actually executed

Commands use pinned Node 24.21.0 and pnpm 12.4.2 via `node scripts/record.mjs <script>` with `ORVIA_TASK_ID=A06`. The index retains all attempts, source manifests, dirty state and exit codes.

| Command | Final exit | Actual result / artifact under `handoffs/codex/artifacts/` |
|---|---|---|
| `install --no-frozen-lockfile` | 0 | Updates lockfile for the selective override; subsequent Docker build installs it frozen |
| `dependencies:check` | 0 | 478 entries, zero matches in 7,408 reviewed npm advisories; `A06-dependency-advisories-1789594963083.json` |
| `db:migrate` | 0 | Append-only 0013 and 0014, retained checksums |
| `contracts:generate`, `contracts:check` | 0 | Existing 41 routes / seven error examples; test endpoint implementation metadata updated |
| `build`, `typecheck`, `lint`, `test` | 0 | Host build `lsXD1v88Zx5OjJrG6HFha`; 11 unit controls |
| `test:auth` | 0 | 87 PASS; `A06-auth-security-1789595731360-c6922d01-5e7b-44d6-b247-9b2067f8973e.json` |
| `test:consent` | 0 | 50 PASS; `A06-consent-integration-1789595836344-b7cfc0bf-d06b-4948-a584-a2b4bd694655.json` |
| `test:expiry` | 0 | 87 PASS, 15 lock waits; `A06-expiry-integration-1789595952818-dd4d1a34-b602-478c-b1d4-4e7802aa9023.json` |
| `test:workflows` | 0 | 32 PASS; `A06-workflow-integration-1789596071898-5f30eaa1-1871-4b7f-a963-4e51718b57af.json` |
| `test:enforcement` | 0 | 46 PASS; `A06-send-enforcement-1789596104116-bec50bb7-e507-4fd5-9543-3fabf6531d73.json` |
| `test:evidence` | 0 | 63 PASS; `A06-evidence-integration-1789596365050-1ca288f5-652e-415c-acc5-16c2fe62bb55.json` |
| `test:isolation` | 0 | 13 PASS; `A06-fixture-isolation-1789595276696-9295bcfa-586c-4be1-a88c-863c48cca115.json` |
| `test:regression` | 0 | 70 PASS; `A06-regression-integration-1789611831642-6fc93594-4e89-4297-a8b0-af685ded1ac1.json` |
| `runtime:build` | 0 | `A06-runtime-image-1789611461052-aa2b0ab0-901c-4921-84cd-790c0c8e3318.json` |
| `test:network confirm:codex-a00` | 0 | 13 boundary controls plus real six-assertion healthy core; `A06-network-qualification-1789611930729-c7f69ce6-3368-4e05-95ea-3b02d173513e.json` |
| `hygiene:check` | 0 | 893 candidate files, nine browser bundles, 40 exact local credentials compared, zero findings; `A06-hygiene-check-2026-09-17T02-28-36.338Z.json` |
| `git diff --check`, staged diff check | 0 | No whitespace errors |

## Observations and corrected results

Original ordinary failures are preserved: stopped Docker engine; missing fault-table forced RLS (corrected by 0014); MFA 429 during rapid sequential fixtures (confirmed in auth audit, corrected by observing the real idle window without changing authentication); initial canary readiness failure (bounded readiness added); and a three-second interrupted-run startup limit on a slow host. That last test now waits up to 30 seconds for actual durable RUNNING before killing the runner. The pending request left by the failed readiness attempt completed through the normal operator with six PASS assertions; no result was fabricated or database reset. Type/lint iterations and all earlier failed wrappers remain in the index.

The broken-control scenario is distinct: its persisted result is FAIL with five PASS and one detected violation, while the surrounding integration test correctly passes. Healthy and repaired runs each have six PASS assertions; restoration has 16 PASS, with its actual snapshot/envelope/receipt/readback journal. Interruption yields ERROR, never PASS. The final regression and network run occurred after all implementation edits. Earlier auth/consent/expiry/workflow/send/evidence/static checks preceded only subsequent test coordination/diagnostic changes; this is not a claim every suite ran after the final commit.

Final tested Docker source SHA-256: `468d6ea98e31fa814d37c8e10c7ff789deeb6227fbcfacbd44533a9a689009a1`; image ID `sha256:8382c994a18d0c86c60415c4d5dd3737ad17d4ea3083aa22ea9859f7f8a0cc21`; container build ID `o1c71ffGFlaerrFTAqBYK`. It captured the then-dirty source on base 62ece0b; its manifest records every byte. An earlier build wrapper correctly records source changes during export; that earlier image is superseded by this final image.

## Contract / dependency / ownership

No endpoint/DTO semantic change; transport 0.4.0 remains canonical. Migrations 0013/0014 are additive. Exact selective dependency override and lockfile are committed. No UI or Work tracking/review file is edited in this increment. UI consumers can now enqueue/read the existing test interfaces, showing NOT_RUN until the protected operator executes them, real FAIL for the broken fixture and explicit ERROR on interruption.

## Limitations and next dependency

No browser acceptance, host-wide network restriction, TLS, fresh-profile install, full control-plane restore, production release signing, comprehensive advisory certification or human rehearsal is inferred. Dependency checks cover the downloaded reviewed npm feed, not every possible vulnerability or container OS package. An interrupted runner with child services already started can require cleanup of those owned processes. Business reset remains unsupported and refused. No customer records, real messages, public deployment or shared-main merge occurred.

Review the A06 PR and evidence; human owns merge, Work owns acceptance. Continue the authorized A07 fresh-profile/TLS/package/frozen-candidate implementation after publishing this increment. W02/W03, unfinished UI/browser work and two human rehearsals remain gates. Preserve and separately publish existing Claude UI files under the human-recorded ownership transfer.
