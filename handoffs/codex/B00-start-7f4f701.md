# B00-B06 UI/browser continuation

Base: `7f4f7010a908f77c036ab31951ed89fa200f722c`, fetched human main. Branch: `prototype/codex/B00-B06-ui`. Current ticket B00, followed serially by B01, B02, B03, B04 and B06 under the attached human assignment. B05/A08 remain excluded. Work alone updates acceptance and canonical trackers.

Inventory: 16 preserved UI source files supply the root, shells, status/form primitives, generated-client hooks and principal sign-in/choices/receipt pages. Workspace has a layout but no pages; tests/e2e does not exist. Existing CSS, Privacy Centre and provenance will be retained. Backend A00-A07 is present, transport 0.4.1 and signed commands 0.3.0; 41 routes remain authoritative. No replacement backend or DTOs are planned.

Allowed writes: UI routes/components, tests/e2e, UI execution notes and Codex handoffs. Only verified UI-blocking defects justify small changes in other Codex-owned paths. Work/source/copy/acceptance files remain read-only. Read the approved master UI sections 102-104 and current source alignment, execution plan, ownership, contract, task definitions, C00 source/copy review and acceptance journeys. Runtime schemas win over stale tracker producer-status descriptions.

Implementation/test order: finish shell/identity/error/pagination foundations; staff MFA/configuration; preserve and correct principal uncertainty/receipt handling; operational views and real Test Lab; serial Playwright journeys on the existing synthetic HTTPS rehearsal profile; consolidated source review, corrections, final browser regression and a new exact package identity. Use the existing pinned Playwright 1.63.0, no dependency upgrade. Record real initial failures and reruns. No customer data/reset, public deployment, real messaging or cloud runtime.

Initial inspection found UI gaps: incomplete pagination, response data retained after a denied refresh, no session recheck on browser return/expiry, mutation responses not invalidated when identity changes, and uncertain consent writes can acquire a new interaction after a read without settling the original request. B00/B02 will correct these without changing server consent semantics. C00 requires separate workflow/obligation count units and genuine 33-module register distinct from connector capabilities.

Browser prerequisite: existing rehearsal CA is not trusted in either current-user or machine Windows roots. Its public thumbprint is `8C592FC41BBD6AA18F42234085F6B8155466A190`. Temporary current-user trust plus exact cleanup was requested from the human; UI implementation continues while pending. Certificate verification will not be bypassed. Playwright browsers and `npx`/GitHub CLI are installed; pinned tools and current GitHub authentication were verified. Other worktrees/profiles are not modified.
