# Handoff — A00 / W00-F07 — Codex — bounded correction

**Base commit:** `e839b1a0e9358d389c5cb728648b1590f8f7ef9e` (human-merged PR #3).
**Review input:** `origin/prototype/work/W00-a00-review` at `beec1a0`; read-only, not merged by Codex.
**New commit:** The containing implementation commit; `A00-F07-results.json` in the subsequent evidence commit records its exact SHA and retests.
**Source master / hash verified:** Approved supplied original, revision 1.3, SHA-256 `527daa1d6a2a7564a61d0375e540ca66b1bc8f33f4e71d327b0f6cb0bf6dbef6`.
**Contract version:** Coordinated executable proposal 0.2.1, pending Work acceptance.
**Scope and profile:** A00 pure-domain correction and approved source placement; synthetic fixtures only, no service mutation.

## Ownership and plan recorded before edits

The user's current instruction authorizes the bounded addition of the approved, unmodified original at `docs/source/ORVIA_Version_1_Unified_Master_with_Version_2_AI_Roadmap.md`. Codex will copy only the hash-verified reference from `C:\Cyberfyx-projects\ORVIA_Version_1_Unified_Master_with_Version_2_AI_Roadmap.md`, without rewriting its content. This is a one-time delegated placement; human ownership of source originals remains in force. Git attributes will preserve the exact approved bytes.

Codex-owned changes are restricted to the completion predicate, canonical contract version/documentation and generated artifacts, public signature fixture, unit regression, necessary test/evidence commands, engineering clarification and this lane's handoff/evidence. Work-owned review/state/tracking and Claude Code UI remain untouched.

Execute the exact W00 counterexample against the unfixed code and retain its expected failure; add a permanent regression; restrict independent completion to a fresh scoped read while retaining provider assertions and execution history; regenerate 0.2.1 artifacts; commit, run W00's requested checks on the correction SHA and verify the committed master hash; publish the correction and factual results. A01/B00 remain gated by Work acceptance and human integration.

## Delivered

`obligationSatisfied` now requires `SCOPED_READ` for `CURRENT_SCOPED_OBSERVATION`. A fresh provider assertion remains valid evidence and reconciliation input but cannot complete that obligation. No execution history is rewritten. Schema documentation carries this distinction into OpenAPI/types, while provider receipt records remain structurally valid. Rejecting their entire record would lose evidence and would prevent the requested NEEDS_ATTENTION result.

Permanent unit cases cover ACK and EFFECT_UNKNOWN with provider-only evidence, no mutation of evidence/history, fresh/expired/future/unsatisfied/current-scope reads, manual attestation separation and retaining provider-based reconciliation. The exact Work reproducer is copied without edits into this lane's four-level `repro/W00-F07/` directory so its original relative import still resolves. Its source is `beec1a0:docs/reviews/work/repro/W00-F07-provider-receipt.mjs`; Work files remain untouched.

The approved original is now present at its requested source path with the expected SHA-256. `.gitattributes` preserves its original bytes/formatting through Git. This is a copy of the approved document, not a source amendment. Existing inventories/state describing the earlier absence are historical; Work must consolidate current state.

## Commands actually executed

The pre-fix `node scripts/record.mjs test:f07` exited **1**: provider-only actual COMPLETED versus expected NEEDS_ATTENTION; ACK-only, fresh-read and stale-read controls passed. This is the expected reproduction of F07, not a healthy-suite pass or later T24 application fault fixture. Report: `artifacts/A00-test-f07-2026-09-16T12-55-42.526Z.json` and its raw log.

The initial pnpm vector-generation invocation hit dependency-directory permissions (exit 1). The subsequent generator correctly rejected the stale 0.2.0 public vector (exit 1, `artifacts/A00-contracts-generate-2026-09-16T12-57-57.048Z.json`). Pinned Node with its supported `--import tsx` loader generated the new public-only vector (exit 0); `contracts:generate` then passed (exit 0, `artifacts/A00-contracts-generate-2026-09-16T12-58-24.241Z.json`): eight artifacts, 37 route examples, seven error examples, version 0.2.1. No private fixture key was exported.

Exact post-commit unit/counterexample, contract drift, typecheck and lint results are recorded in the companion `A00-F07-results.json`, with the source SHA, source cleanliness and actual command reports. Old evidence is retained without relabelling it as a run on this correction.

## Contract / dependency / ownership changes

Contract 0.2.0 → 0.2.1, including manifest version, generated schema literals and signature vector. No route/field/enum removal or new runtime endpoint; no dependency pin/lockfile or database migration change. Existing 0.2.0 signed fixture envelopes are invalid under the 0.2.1 schema and must be reissued. The generator remains the sole source of OpenAPI/types/examples. A03/A05 consume the corrected predicate; B00/B03 consume the shared generated documentation/types after acceptance. No UI changes or new ownership transfer.

The evidence wrapper now reads the contract version from its package manifest, fingerprints the copied reproducer and distinguishes dirty source from evidence-only additions. This supports truthful retests on the fix commit.

## Acceptance, remaining limitations and next integration action

This fixes the two requested implementation/source-placement items; Work owns the finding-closure and A00 acceptance decision. Review the exact correction and post-commit evidence, then have the human integrate it. Do not interpret PR #3's earlier merge or this correction push as accepted A01/B00 authority.

Full T01–T34 application scenarios, live service/recovery/reset, browser acceptance and advisory audit are NOT_RUN in this bounded correction. No services or customer data were changed. A01–A07, runtime authority/observation matching and production qualification remain gated as previously recorded. Work's review branch/template was read from Git without merging or editing it. Retest relevant checks on the human-integrated SHA before starting A01/B00.
