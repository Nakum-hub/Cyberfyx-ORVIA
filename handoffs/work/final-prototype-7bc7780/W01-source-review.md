# W01 consolidated source intake — acceptance pending runtime retest

Reviewed integrated base `7bc7780de51c095ccd808ef5d01e423106c65f30` against approved master sections 16–18, 35–36 and the prototype contract. The user-assigned Work reviewer is also performing the bounded correction execution in this task; this is not an independent penetration test or human acceptance.

Historical W01-A02-F01 is **corrected in supplied source, pending this review's execution decision**. `packages/domain/src/consent.ts` takes the consent advisory/aggregate row lock, interaction row lock and shared publication lock before a single-use guarded update using materialized `clock_timestamp()`. It then writes epoch/event/receipt/workflow/outbox in the surrounding transaction. `configuration.ts` likewise consumes the exact publication proof after its relevant locks using advancing server time. Authorized identical replay in `transaction.ts` returns the saved response before a new-operation consumption check. Historical failures remain retained; the previous open finding is not erased by its merge.

`tests/integration/consent/expiry.test.ts` exercises actual HTTP and PostgreSQL blocking: it verifies transaction/wait began before expiry, releases after observed expiry and compares committed rows/effects. Existing A07 handoff reports 87 checks including 15 actual waits; those old results retain their source identities. A new execution against this continuation remains required before consolidated closure.

`business.ts` validates server-derived domain/capability/scope, bounded input, same-origin JSON writes and idempotency keys. It rechecks authority after transaction entry. `authorityFor` rejects ambiguous simultaneous staff/principal sessions, bearer-shaped human requests, inactive authority and privileged sessions without the recorded MFA ceremony. `scopedTransaction` validates the DB role is non-superuser/non-BYPASSRLS and sets transaction-local scope. These are source findings, not substitutes for auth/consent/isolation tests.

Current browser evidence: baseline 14 failures at inaccessible exact Email/Password labels and one normal TLS pass. The shared Field correction passed the first real MFA/refresh/domain/logout journey. The following auth rejection test exposed global alert ambiguity with Next's route announcer; the test is now scoped to main without changing expected denial. All three auth journeys and distinct-reviewer persisted configuration have subsequently reported PASS in the running neighboring suite; its final report is still required.

W01 remains BLOCKED until the full relevant consent/browser and expiry regression evidence is reviewed. Manual-task version correction belongs to W02/B03 integration and is separately recorded; no final candidate, canonical T result or human sign-off is promoted here.
