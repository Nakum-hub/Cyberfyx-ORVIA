# ORVIA continuation prompt

Continue the existing ORVIA prototype completion task in `C:\Cyberfyx-projects\Cyberfyx_ORVIA`, GitHub `Nakum-hub/Cyberfyx-ORVIA`. Use the repository, terminal and real local browser/runtime. Do the remaining work; do not rebuild the application or return another plan.

The human paused Codex to conserve credits and explicitly requested this local/GitHub checkpoint for continuation in Claude Code or Cowork. Read this directory's `CHECKPOINT.md`, `command-index.json`, `bounded-corrections.md`, `W01-source-review.md`, `W02-source-review.md`, and `ORIGINAL_REQUEST.md` first. The last file preserves the full original completion brief and required final gates. Follow AGENTS.md and its approved-master reading order.

Start by inspecting Git status, fetching GitHub, and comparing latest human-integrated main with branch `codex/final-prototype-review` and its review PR. **Do not discard this branch's corrections by starting over from main.** If the checkpoint is unmerged, resume it or create a continuation branch containing it; reconcile newer main without merging yourself into main. Existing other worktrees and stash must remain intact. Inspect the PR's actual head, not a SHA guessed from this prompt.

You are continuing the human-assigned integration/review role. The original brief explicitly permits existing tests and the smallest verified fixes, including the manual-attestation contract gap. Record any further bounded ownership transfer before editing engineering paths; no unrelated redesign. Work-owned canonical reviews/tracking/C documents are still stale and require evidence-backed consolidation. This handoff does not grant human sign-off, release approval, database-reset permission or new OS trust approval.

Current correction: transport contract 0.5.0 (signed commands remain 0.3.0), authoritative Obligation.task_version from stored manual_version, generated types/OpenAPI, assigned-member manual UI using that exact version, preserved original request during uncertain response/refresh, concurrent/replay integration assertions, accessible exact labels, scoped application-alert test locators, browser packaging metadata correction, and browser suite ownership-connection heartbeat. No dependency/lockfile or migration change.

Resume from the actual latest Test Lab failure recorded in CHECKPOINT.md. The earlier full browser run had 14 passes and two Test Lab failures due to the idle ownership connection. Its abandoned run was recovered through the existing runner as ERROR, without resetting or rewriting history. A targeted rerun after the heartbeat exposed a separate assertion: the deliberately broken run returns process exit 0 while the browser test expects 1. Inspect `scripts/regression-runner.ts`, `tests/e2e/test-lab.spec.ts`, and `tests/integration/regression/regression.test.ts`; decide the documented CLI exit contract from real behavior. Preserve the business run's actual FAIL and detected violating send. Do not turn that business FAIL into PASS, invent success, or weaken a correct product assertion. The heartbeat itself still needs a successful full run beyond five minutes.

The manual browser regression passed, including real lost response, identical replay, stale concurrent tab 409, exactly one version increment, attribution, and no invented independent observation. The updated backend evidence integration suite has NOT_RUN in this continuation. Existing static checks and production build passed at the recorded dirty snapshots; no new final candidate has been frozen. Original package manifest still names historical candidate `766854399d411c551c4d3f657ea2b4f3f189a4bd` and must not be represented as the 0.5.0 candidate.

The final targeted Test Lab run finished with both cases failing. Its second case also references nonexistent `app.purposes` in the interruption barrier, and setup happens before the cleanup try/finally. Correct that to the actual configuration table after inspecting the schema, and guarantee rollback/release even if barrier setup fails. The leaked test connection caused teardown timeout. Check `pause-closeout.json` for the exact queued NOT_RUN record; preserve it and use supported execution/recovery, never delete or manually mark it passed. No application worker/agent should remain active at pause; verify before resuming.

Use only the existing `rehearsal` / `aster-birch-v1` synthetic environment. Existing Docker service volumes and consent history are retained. Protect `.local` credentials, keys, raw authenticated traces and populated stores. Never print or publish them. Run suites serially against exclusive database/ports. Use the existing recorders and retain every failed attempt. `run-engineering.ps1` in this directory is a prepared serial helper, NOT evidence that those commands ran. Inspect its sequence before execution. Do not rerun `recover-interrupted.ts` blindly: it is pinned to one already-recovered run and deliberately refuses other pending work.

Temporary Windows CurrentUser Root trust was explicitly approved and installed for certificate thumbprint `8C592FC41BBD6AA18F42234085F6B8155466A190`. Check `certificate-trust-removal.json` for the pause cleanup. If it records successful removal, normal Chromium will need **fresh explicit human approval** before reinstalling that exact still-valid CA (recheck subject, issuer, fingerprint and expiry). Never use ignore-certificate-errors, ignoreHTTPSErrors, plaintext fallback or machine-wide trust. Keep per-process Node CA use distinct from OS/browser trust.

Then complete the remaining original sequence: reproduce/fix/retest Test Lab; all seven browser suites; updated evidence integration and applicable auth/consent/expiry/workflow/enforcement/regression/TLS/lifecycle/network/hygiene checks; W01 expiry finding disposition and authority review; W02 integrated failure/security/recovery review; commit and refreeze using existing packaging tools; independently verify bundle/archive/hash identities; execute/reconcile each complete T01–T30 scenario on that exact candidate; refresh C01/C02 and their existing builder/validator; conduct two separate real human-run or supervised rehearsals; W03 and human final sign-off. A08/B05 and T31–T34 remain optional and unpromoted. Human retains merge/release authority.

Report exact source/build/contract/profile/fixture, commands, UTC intervals, exit codes, assertions and artifacts. Source presence, engineering PASS, Work acceptance, full scenario acceptance and human rehearsal are different facts. Never relabel an older source's evidence as the new candidate. Continue all executable work and ask the human only for genuinely human-only steps. Internal-demo readiness remains NOT_READY until mandatory gates actually pass.
