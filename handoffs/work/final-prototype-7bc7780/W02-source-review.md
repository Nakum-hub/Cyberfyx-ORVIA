# W02 consolidated source review — execution decision pending

Base: `7bc7780de51c095ccd808ef5d01e423106c65f30`; bounded 0.5.0 correction is an uncommitted, hash-recorded working revision. Reviewer: user-assigned Work lane, also performing the explicitly authorized bounded engineering correction. This is not an independent penetration test or human release approval.

| Boundary | Source inspected / conclusion | Required execution evidence |
|---|---|---|
| Durable workflow and command | `packages/domain/src/workflow.ts` persists one plan and obligation set and signs a one-record, one-attempt command. Existing plans prevent duplicate planning. Worker recovery must still be demonstrated. | workflow suite, durable outbox/Temporal interruption and stable command replay |
| Target effect | `apps/agent/src/execute.ts` verifies signature, installation, scope, enrolled operation/capability and budgets. It locks current consent and the exact target, checks policy approval, epoch and generation, and verifies expiry again immediately before mutation. The target ledger returns an identical committed receipt without repeating mutation. | workflow command-denial controls and actual target before/after reads |
| Current admission | `packages/domain/src/processing.ts` checks enrolled sender/system, current consent and publication under locks, mapping generation, quarantine, unresolved suppression and the separate service condition. A replay returns the committed send response without a new send row. | enforcement ordering, independent-purpose and dependency failure controls |
| Policy failure | `packages/policy-sdk/src/index.ts` returns INDETERMINATE for timeout, transport failure, missing result and malformed strict result. There is no permissive fallback. | actual OPA missing/malformed/outage and recovery controls |
| Observation and completion | `workflow.ts` reads the exact synthetic resource separately and records denied/unavailable reads as UNVERIFIABLE. `completion.ts` requires current scope and a fresh SCOPED_READ with matching desired/observed state, or a separately declared attributed-manual criterion. Empty coverage is NEEDS_ATTENTION. | evidence suite and browser ACK-without-effect, uncertainty, manual and stale-scope cases |
| Reconciliation | `packages/domain/src/evidence.ts` persists a read request, does not replay a target mutation, retains the original attempt, and completes only from current read evidence. | stopped-worker persistence, restart, immutable unknown attempt and read-permission-loss controls |
| Manual action | `attest` retains its row lock, assignment/RLS authority, exact task-version check, current-consent lock/recheck and workflow receipt binding. The new read field uses stored `manual_version`; the UI uses that value and retains pending/unsettled request state through refresh. | integration concurrency/replay/evidence denial plus assigned-member browser regression |
| Export and counts | Evidence is composed from scoped persisted workflow/event/test records, audits reads/exports, includes unresolved limits and a bounded digest claim. Overview derives counts from those records. | exact export digest, scope/role denials, audit and DB count comparisons |
| Target restore | `packages/testing/src/target-recovery.ts` is an operator-only helper, requires the current run-owned snapshot and installation/profile identity, commits target quarantine first, and only activates after exact current withdrawal epoch/generation/restriction checks. No control-plane rollback is claimed. | actual Test Lab target-only restore and interrupted-run tests |
| Runtime egress | Existing network qualification isolates the backend on an internal Docker network with denied DNS and a separate canary. Existing Playwright network records are explicitly scoped to its observed context. | fresh image/source match and blocked-egress backend run; browser-context coverage must be assessed without expanding the claim to all host traffic |
| Transport/lifecycle | Existing TLS tests verify trusted chain, wrong-host/untrusted rejection, secure cookies and no HTTP fallback. Lifecycle refuses a business-schema reset and checks supervised process restarts preserve counts. | actual TLS and lifecycle suites; no database reset is authorized by this review |

The manual browser regression passed at `B06-playwright-2026-09-17T10-38-05.694Z/results.json`. Its screenshot was visually inspected: attributed administrative closure is readable and explicitly distinct from automated observation. Full browser execution and fresh backend regressions are still running/pending; this document does not close W02 or promote canonical T statuses.

Retained limitations: synthetic targets only; one effect attempt followed by read reconciliation; no production signing, whole-host egress assurance, full control-plane disaster recovery, enterprise security certification or human rehearsal inferred from engineering execution.
