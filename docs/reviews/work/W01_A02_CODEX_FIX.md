# Codex prompt — finish A02's freshness correction

```text
You are the Codex lane for Nakum-hub/Cyberfyx-ORVIA. Address Work finding W01-A02-F01 from docs/reviews/work/W01_A02_REVIEW.md. Read AGENTS.md, CURRENT_STATE.md, the approved master, CONTRACT.md, FILE_OWNERSHIP.md and the review. A01 remains accepted at 50cb4daeded9253c4f7cca4f742cb212c10aa5b7. Reviewed A02 implementation is 3242521e59966885d8053747a82d96cb92ea55d5, publication 2e01b7a36d663382d82ec9310bd707f1c2d405ef, human integration 004fe3dc43e3a278caeb9c4e4983e7e58a825b9f; Work inspected a5b6ff73c4fca4aa02e110ee5f11a121b1a7563b. Candidate contract is 0.3.0.

Preserve your current A03 work and the human-authorized A02-A07 continuation. Record your actual current base and isolate the correction; do not reset/discard another ticket or lane. First verify whether the finding is still present in your newer source. Work identified it by source review; Work could not run PostgreSQL or the application reproduction. Do not describe it as an already executed Work test.

The defect: publishPolicy consumes publication proof with expires_at>now() after publication/purpose lock waits. changeConsent checks interaction expires_at>now() after aggregate locking and before a possible later shared-publication lock wait. PostgreSQL now() is transaction-start time, so a new request begun just before expiry can consume authority after its deadline. A check before another blocking wait is also insufficient.

Implement a small fix in your owned configuration/consent/transaction/server code as necessary: define the actual consume/mutation boundary and recheck proof/interaction expiry using an advancing trusted server clock after the relevant waits/row locks. Do not simply move the same transaction-start check, use statement-start time before another wait, blanket-rewrite event timestamps, weaken expiry, or change historical receipts. Preserve session/actor/scope/version/digest binding, single-use proof, current authority, aggregate locking and transactional rollback. Authenticated identical replay of an already committed operation must still return its original response after expiry; no new side effect is allowed.

Add and run real HTTP/PostgreSQL regression controls in your existing isolated synthetic codex-a00 test profile: fresh success; already-expired denial; publication proof expiring while the publication lock is held; consent interaction expiring during aggregate/interaction lock waits; interaction expiring during the later shared-publication wait. Use only test-owned records, bounded synchronization and no shared reset. Check actual wait/expiry ordering and database outcomes, not status codes alone. An expired new operation must produce no approval/policy transition, consumed success, epoch advance, consent event/receipt, workflow/outbox or idempotency-success record. Verify original committed replay still succeeds and preserves its prior response. Keep existing scope/MFA, self/wrong-digest/session approval, immutability, atomic rollback, conflicting idempotency/epoch and stale-grant/fresh-reconsent controls.

The Work SQL file docs/reviews/work/repro/W01-A02-F01-expiry.sql is an optional temporary-table clock-semantics control, explicitly NOT_RUN by Work. It is not a substitute for exercising the actual HTTP/domain/PostgreSQL paths. Record the pre-fix observation or contrary executed evidence, then the corrected retest without deleting prior failures.

Keep the current stack, approved master, ownership and local synthetic boundary. No UI takeover, AI/vendor operations, dependency change or production credentials are requested. Leave applied migrations immutable. If interface semantics must change, submit a coordinated version/schema/generated-client handoff; otherwise preserve the 0.3.0 shapes and update your engineering description of the freshness boundary.

Run the relevant consent/auth regressions, unit tests, contract drift, typecheck, lint and build on the exact final submitted source, with command exits, hashes, fixture/profile/build identity and logs. Complete handoffs/TEMPLATE.md under handoffs/codex/, publish the correction commit/PR and return it to Work. Do not edit Work's review/tracking/state files or self-promote A02/W01/full acceptance. Human owns merges and release. Continue the previously authorized coding sequence without treating this review or a green unit suite as final acceptance of A03-A07.
```
